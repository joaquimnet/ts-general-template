{
  "name": "typescript-lib-template",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "swc src -d dist --copy-files --delete-dir-on-start",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "rome check src --apply-suggested",
    "format": "rome format src --write",
    "ci": "tsc --noEmit && rome ci src && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.18",
    "@swc/helpers": "^0.4.14",
    "rome": "^10.0.1",
    "typescript": "^4.9.3",
    "vitest": "^0.25.2"
  }
}
